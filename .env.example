# Copy to .env and fill in. .env is gitignored.

# Brave Search API — free tier at https://brave.com/search/api/
# Only needed for `--connector brave`; the default fixture connector needs nothing.
BRAVE_SEARCH_API_KEY=
